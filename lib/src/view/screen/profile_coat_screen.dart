import 'package:booking_app/src/view/screen/main_bottom_nave_screen.dart';
import 'package:flutter/material.dart';

class ProfileCoatScreen extends StatelessWidget {
  const ProfileCoatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: Text('askjkfa'),),
      body:Column(
        children: [


        ],
      ),
    );
  }
}
